- name: vars prefer 
  hosts: local
  connection: local
  vars:
    NAME: from play level
  vars_file:
  - course.yaml
  vars_prompt:
  - name: NAME?
    prompt: What is your NAME?
    private: false
  tasks:
  - name: Testing var perferance
    vars:
    NAME: from play level
    ansible.builtin.debug:
      msg: "name: {{ NAME }}"
  