- name: varas args
  hosts: local
  connection: local # ansible is connecting to localhost
  vars:
    number: 5
  tasks:
  - name: check number is less than 10 or not
    ansible.builtin.debug:
      msg: "Given number is: {{ number }} is less than 12"
    when: number < 12

